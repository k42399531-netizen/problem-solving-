let products = ["Laptop", "Mouse", "Keyboard", "Monitor"];
let found = false;
// loop
for (let i = 0; i < products.length; i++) {
    if (products[i] === 'camera') {
        found = true;
        console.log("the found is " + found);
    }
    else {
        console.log("the found is " + found);
    }
}